import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class MindstormScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
     appBar: AppBar(
       title: Text( 'Curso de MINDSTORM'), 
        toolbarHeight: 90,
         elevation: 14,
         backgroundColor: Colors.purpleAccent,
      ),

      body: Column(
      ),
    );
  } 

}

